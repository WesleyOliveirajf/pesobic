name: Vercel deployment configuration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - name: Explain Vercel deployment
        run: |
          echo "This repository deploys through its Vercel Git integration."
          echo "Set VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY in Vercel Project Settings."
